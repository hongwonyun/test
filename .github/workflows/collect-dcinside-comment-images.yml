name: Collect DCInside comment images

on:
  push:
    branches:
      - chatgpt-temp-dcinside-352852-a24
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out temporary branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check requests beautifulsoup4 lxml

      - name: Collect comment images
        id: collect
        continue-on-error: true
        run: python .github/scripts/collect_dcinside_comment_images.py

      - name: Upload collected ZIP and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rescene1-352852-comment-images
          path: |
            out/**
            work/debug/**
          if-no-files-found: error
          retention-days: 1

      - name: Mark workflow failed when no image was saved
        if: steps.collect.outcome == 'failure'
        run: exit 1
